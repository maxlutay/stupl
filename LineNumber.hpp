#pragma once

using LineNumber = int64_t;

/*

#include <errors.h>

class LineNumber : public int64_t {
  LineNumber(int64_t value) {
    if (value < 0)
      throw std::logic_error("LineNumber should be >= 0");
    int64_t{value};
  };


};

*/
